# Synthetic Examples

The demo's built-in examples are generated from deterministic random seeds at
runtime. No real medical images, private patient data, PHI, DICOM headers, or
clinical data are stored in this directory.
